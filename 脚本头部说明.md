# Nai2API 酒馆助手生图脚本 — 头部说明（v24）

> 本文件原为脚本开头的整段注释，v23 起单独存放，保持脚本文件本体的简洁。
> 部署与完整说明见仓库根目录的 [`部署与使用说明.md`](../部署与使用说明.md)。

## 基本信息

- 作者：glm5.2 / glm5.3 / deepseek-v4.1-falsh
- 依赖：JS-Slash-Runner (TavernHelper) + SillyTavern ≥ 1.12.14
- API：`https://nai.sta1n.cn`

## 功能

- 根据聊天情节自动生成英文 Danbooru 标签 prompt（AI 润色，可关）
- 支持用户预置提示词（拼接在 AI prompt 前面）
- 相同 prompt 并发生成 1-5 张图
- 每条 AI 回复后自动生图（可配置开关）
- 手动按钮生图 + 查询余额
- 6 种画风预设 / 3 种朝向 × 3 档分辨率 / 自定义负面 prompt
- 模型可选 `nai-diffusion-4-5-full`（1 点起/张）或 `nai-diffusion-5-full`（8 点起/张）
- **提示词预设**：把"提示词"区三个字段的当前内容存为预设（酒馆全局变量，多端同步），可应用/重命名/删除
- **提示词插入**：多条要求条目（给生图提示词 AI 的指令），可勾选启用、上下排序，整体注入在指令最开头或最末尾
- **英文自然语言模式**：可选让 AI 输出英文自然语言画面描述（适配 NAI 5，替代已不可用的中文转提示词接口）
- **并发生图失败隔离**：部分成功也照常出图；提交前做余额预检（按当前价目计算所需点数）
- 图片以系统消息（`role=system`）插入聊天，避免成为 AI 续写目标触发 Claude prefill 报错

## 入口

1. 脚本按钮 `🎨NAI生图` — 手动触发
2. 脚本按钮 `💰NAI余额` — 查询余额
3. 脚本按钮 `⚙️NAI设置` — 打开设置面板
4. 自动监听 `MESSAGE_RECEIVED` 事件 — 每条 AI 回复后自动生图（开关控制）

## 部署

1. 在酒馆助手脚本库里新建脚本，粘贴脚本文件全部内容
2. 在脚本编辑器里添加三个按钮：`🎨NAI生图`、`💰NAI余额`、`⚙️NAI设置`（名称需与脚本内 `getButtonEvent` 完全一致，含 emoji 与空格）
3. 启用脚本，打开 `⚙️NAI设置` 填入 API Key

## 调试

脚本顶部有 `const DEBUG = false;` 开关：

- 日常使用保持 `false`（默认）：控制台只留一行加载提示
- 排查问题时改为 `true`，会输出**完整、不截断**的排查信息：
  - `▶ 发送给 AI 的完整 sysPrompt` — 含整段情节上下文与生成要求
  - `◀ AI 原始输出` / `◀ 剥离思维链后`
  - `▶ 发送给生图模型的完整 prompt` / `negative`
  - 以及缓存、批量结果、余额预检等过程日志
- 注意：DEBUG 开启时单条日志可达上万字符，浏览器开发者工具会把这些字符串留在内存里；长时间挂机 + 自动生图会持续占用内存，排查完记得改回 `false`
- 脚本加载时会始终打印一行提示，说明当前调试状态
